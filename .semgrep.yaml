rules:
  - id: ruby-command-injection
    pattern: system("... #{...} ...")
    message: Possible command injection via string interpolation in system call
    languages: [ruby]
    severity: ERROR
    metadata:
      category: security
      cwe: "CWE-78"

  - id: ruby-unsafe-eval
    pattern: eval(...)
    message: Use of eval() is dangerous and can lead to code injection
    languages: [ruby]
    severity: WARNING
    metadata:
      category: security
      cwe: "CWE-94"

  - id: ruby-unsafe-yaml-load
    pattern: YAML.load(...)
    message: Use YAML.safe_load instead of YAML.load to prevent deserialization attacks
    languages: [ruby]
    severity: ERROR
    metadata:
      category: security
      cwe: "CWE-502"
